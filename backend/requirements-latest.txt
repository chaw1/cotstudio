# COT Studio MVP - 最新版本依赖
# 移除问题包，使用最新稳定版本

# FastAPI核心依赖 - 使用最新版本
fastapi
uvicorn[standard]
pydantic
pydantic-settings

# 数据库相关
sqlalchemy
alembic
psycopg2-binary
asyncpg

# 异步任务队列
celery
redis

# 图数据库
neo4j

# 认证和安全 - 移除问题包
# jwt==1.4.0  # 问题包：过时且与PyJWT冲突
# jose==1.0.0  # 问题包：过时且与python-jose冲突
PyJWT[cryptography]  # 使用最新版本
passlib[bcrypt]
python-multipart

# 文件处理
python-magic-bin  # Windows兼容版本
Pillow
pdf2image

# 安全扫描 - 可选包，可能导致编译问题
# yara-python  # 问题包：需要编译，在Windows上可能失败

# OCR处理 - 大型包，可能导致网络超时
# paddlepaddle  # 问题包：体积巨大，下载容易超时
# paddleocr  # 问题包：依赖paddlepaddle

# HTTP客户端
httpx
aiofiles

# LLM集成
openai
# langchain==0.0.350  # 问题包：版本过旧
# langchain-openai==0.0.2  # 问题包：版本过旧
langchain
langchain-openai
tenacity

# 对象存储
minio

# 开发和测试
pytest
pytest-asyncio
pytest-cov
black
isort
flake8

# 监控和日志
structlog
psutil