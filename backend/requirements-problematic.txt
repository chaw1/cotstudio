# COT Studio Backend - Problematic Dependencies
# These packages may fail with pyproject.toml but work with direct pip install

# Database drivers that may have build issues
psycopg2-binary==2.9.9
asyncpg==0.29.0

# Security scanning (may have compilation issues)
yara-python==4.3.1

# Additional LLM dependencies (may have build issues)
tiktoken==0.5.1